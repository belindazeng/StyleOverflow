<%= simple_form_for @question do |f| %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>

<ul class="list-group">
	<% @testing.each do |question| %>
		<li class="list-group-item">
			<%= question.content %> 
	<% end %> 
</ul>